# Adding a Stage

1. Extend `config/examples/pipeline.yaml`.
2. Add or update any labels required in `labels.yaml`.
3. Implement the stage behavior in `src/hermes/pipeline/engine.py`.
4. Add tests covering the new stage rules.
5. Update architecture and onboarding docs.

