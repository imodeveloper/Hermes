# Adding a Repo Config

1. Add the repo entry to `repos.yaml`.
2. Set local path, default branch, worktree root, validation commands, and release behavior.
3. Ensure the repo path is inside the intended allowlist.
4. Re-run `hermes doctor` and the sandbox flow.

