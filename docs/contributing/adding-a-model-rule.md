# Adding a Model Rule

1. Extend `models.yaml` with the new task class or escalation.
2. Keep prompt budgets explicit.
3. Add tests for the routing result.
4. Document the change in `docs/onboarding/model-routing.md`.

